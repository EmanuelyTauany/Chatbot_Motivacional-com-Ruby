<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chatbot</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-200 h-screen flex items-center justify-center p-4">
    <div class="w-full max-w-xl bg-white flex flex-col h-[700px] shadow-lg rounded-lg overflow-hidden">
    <div class="h-[600px]">
    <h1 class="text-xl font-bold text-center p-4 border-w"> Chat </h1>
    
    <div class="h-[550px] overflow-y-auto p-4 space-y-5 bg-green-100">
    <% @messages.each do |message|%>
     <div class="text-sm justify-center text-grey-300 italic">
      <p><strong><%= message[:name] %>:</strong> <%= message[:msg]%></p>
    </div>
    <%end%>
    </div>
    </div>

    <form action="/send" method="post" class="flex gap-2 p-4 border-t">
    <input type="text"  name="name" placeholder="Seu nome" required
    class="flex-1 border rounded-lg p-2 focus:ring focus:fring-blue-300 outline-none">
    <input type="text" name="msg" placeholder="Difgite sua mensagens ou /inspira" required
    class="flex-1 border rounded-lg p-2 focus:ring focus:ring-blue-300 outline-none">
    <button type="submit"
    class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600">Enviar</button>
    </form>

    </div>

</body>
</html>
