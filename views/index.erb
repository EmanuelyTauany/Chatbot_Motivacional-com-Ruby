<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Chat de Apoio Emocional - Cláudia</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #667eea, #764ba2);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }
    
    .container {
      max-width: 850px;
      width: 100%;
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(16px);
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 10px 40px rgba(0,0,0,0.15);
      display: flex;
      flex-direction: column;
    }

    .header {
      background: linear-gradient(135deg, #667eea, #764ba2);
      padding: 25px;
      text-align: center;
      color: white;
    }
    .header h1 {
      font-size: 1.8rem;
      font-weight: 600;
    }
    .header p {
      opacity: 0.85;
      font-size: 0.95rem;
      margin-top: 5px;
    }

    .emergency-section {
      background: linear-gradient(135deg, #ff4757, #ff6b8a);
      padding: 15px 20px;
      text-align: center;
      color: white;
    }
    .emergency-section h3 {
      margin-bottom: 10px;
      font-size: 1.1rem;
    }
    .emergency-btn {
      background: white;
      color: #ff4757;
      padding: 10px 18px;
      border-radius: 30px;
      margin: 5px;
      font-weight: 600;
      text-decoration: none;
      transition: 0.3s;
      display: inline-block;
    }
    .emergency-btn:hover {
      background: #ffe0e3;
    }

    .chat-container {
      flex: 1;
      overflow-y: auto;
      padding: 20px;
      background: rgba(255,255,255,0.35);
    }

    .message {
      margin-bottom: 18px;
      animation: fadeIn 0.3s ease-in;
    }
    .bubble {
      padding: 14px 18px;
      border-radius: 18px;
      max-width: 75%;
      line-height: 1.5;
      font-size: 0.95rem;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }
    .sender { font-weight: bold; margin-bottom: 4px; font-size: 0.85rem; opacity: 0.8; }
    .time { font-size: 0.7rem; opacity: 0.6; margin-top: 6px; }

    .message.system .bubble { background: linear-gradient(135deg, #667eea, #764ba2); color: white; }
    .message.user .bubble { background: #ffffff; color: #333; margin-left: auto; }
    .message.emergency .bubble { background: linear-gradient(135deg, #ff6b6b, #feca57); color: white; }

    .input-area {
      padding: 15px 20px;
      background: rgba(255,255,255,0.9);
      border-top: 1px solid #eaeaea;
    }
    .form-row {
      display: flex;
      gap: 10px;
    }
    input[type="text"] {
      flex: 1;
      padding: 12px 16px;
      border: none;
      border-radius: 30px;
      font-size: 0.95rem;
      background: #f0f0f5;
      transition: 0.3s;
    }
    input[type="text"]:focus {
      outline: none;
      background: #e6e6ff;
    }
    button {
      padding: 12px 20px;
      border: none;
      border-radius: 30px;
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      font-weight: 600;
      cursor: pointer;
      transition: 0.3s;
    }
    button:hover {
      opacity: 0.9;
      transform: translateY(-2px);
    }

    .button-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      gap: 10px;
      margin-top: 15px;
    }
    .btn {
      display: block;
      padding: 12px;
      text-align: center;
      border-radius: 15px;
      font-weight: 500;
      text-decoration: none;
      color: white;
      transition: 0.3s;
    }
    .btn:hover { transform: scale(1.05); }
    .btn-success { background: linear-gradient(135deg, #26d0ce, #1dd1a1); }
    .btn-info { background: linear-gradient(135deg, #74ebd5, #ACB6E5); }
    .btn-warning { background: linear-gradient(135deg, #f6d365, #fda085); color: #2d3436; }
    .btn-neutral { background: #95a5a6; }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 768px) {
      .container { border-radius: 12px; }
      .button-grid { grid-template-columns: 1fr 1fr; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>💙 Chat de Apoio Emocional</h1>
      <p>Cláudia está aqui para te ouvir e apoiar ✨</p>
    </div>

    <div class="emergency-section">
      <h3>🆘 Precisa de ajuda imediata?</h3>
      <a href="/crisis_help" class="emergency-btn">Ajuda de Emergência</a>
      <a href="/sos" class="emergency-btn">SOS</a>
      <p>CVV: 188 (24h) | SAMU: 192 | Emergência: 190</p>
    </div>

    <div class="chat-container" id="chat">
      <% @messages.each do |msg| %>
        <div class="message <%= msg[:type] || 'system' %>">
          <div class="bubble">
            <div class="sender"><%= msg[:name] %></div>
            <%= msg[:msg] %>
            <div class="time"><%= msg[:time] %></div>
          </div>
        </div>
      <% end %>
    </div>

    <div class="input-area">
      <form method="post" action="/send">
        <div class="form-row">
          <input type="text" name="name" placeholder="Seu nome" value="Você" required>
          <input type="text" name="msg" placeholder="Como você está se sentindo?" required autofocus>
          <button type="submit">Enviar</button>
        </div>
      </form>
      <div class="button-grid">
        <a href="/daily_tip" class="btn btn-success">💡 Dica do Dia</a>
        <a href="/breathing" class="btn btn-info">🌬️ Respiração</a>
        <a href="/meditation" class="btn btn-info">🧘 Meditação</a>
        <a href="/affirmation" class="btn btn-success">✨ Afirmação</a>
        <a href="/alternatives" class="btn btn-warning">🔄 Alternativa</a>
        <a href="/distraction" class="btn btn-warning">🌈 Distração</a>
        <a href="/grounding" class="btn btn-info">🌍 Grounding</a>
        <a href="/self_care" class="btn btn-success">🛀 Autocuidado</a>
        <a href="/hope_reminders" class="btn btn-success">🌅 Esperança</a>
        <a href="/safety_plan" class="btn btn-warning">🛡️ Segurança</a>
        <a href="/professional_help" class="btn btn-info">👨‍⚕️ Ajuda Prof.</a>
        <a href="/emergency_contacts" class="btn btn-warning">📞 Contatos</a>
        <a href="/mood_check" class="btn btn-info">📊 Humor</a>
        <a href="/goal" class="btn btn-success">🎯 Meta</a>
        <a href="/encouragement" class="btn btn-success">🌟 Motivação</a>
        <a href="/clear" class="btn btn-neutral">🗑️ Limpar</a>
      </div>
    </div>
  </div>
</body>
</html>
